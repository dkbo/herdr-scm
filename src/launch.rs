//! Placeholder — Task 17 replaces this with the real launcher decision logic.

/// Always `OPEN` until Task 17 implements the real decision.
pub fn launch_decision(_pane_list_json: &str) -> String {
    "OPEN".to_string()
}

/// Always `OPEN` until Task 17 implements the real decision.
pub fn launch_decision_tab(_pane_list_json: &str) -> String {
    "OPEN".to_string()
}
